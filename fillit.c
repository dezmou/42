#include <stdlib.h>
#include <stdio.h>
int main(){
	char *chien;

	chien = malloc(20);
	chien = malloc(185);
	chien = malloc(12);
	//chien = malloc(84);
	//chien = malloc(98);
	//chien = malloc(5488);

	getchar();
	return 0;
}
